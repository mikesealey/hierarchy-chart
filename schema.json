{
  "type": "component",
  "metadata": {},
  "schema": {
    "name": "hierarchy-chart",
    "friendlyName": "hierarchy-chart",
    "description": "A hierarchy chart (aka Tree Chart) that can be used to show the hierarchy of a business or department.",
    "icon": "tree-structure",
    "settings": [
      {
        "type": "dataProvider",
        "label": "Provider",
        "key": "dataProvider",
        "required": true
      },
      {
        "type": "field",
        "label": "Text 1 column",
        "key": "text1Column",
        "dependsOn": "dataProvider",
        "options": {
          "source": "dataProvider",
          "from": "provider"
        },
        "resetOn": "dataProvider"
      },
      {
        "type": "field",
        "label": "Text 2 column",
        "key": "text2Column",
        "dependsOn": "dataProvider",
        "options": {
          "source": "dataProvider",
          "from": "provider"
        }
      },
      {
        "type": "field",
        "label": "Text 3 column",
        "key": "text3Column",
        "dependsOn": "dataProvider",
        "options": {
          "source": "dataProvider",
          "from": "provider"
        }
      },
      {
        "type": "field",
        "label": "Image column",
        "key": "imageColumn",
        "dependsOn": "dataProvider",
        "options": {
          "source": "dataProvider",
          "from": "provider"
        },
        "info": "Can be a text column as a URL for an image, or a single-attachment column that holds images"
      },
      {
        "type": "field",
        "label": "Parent column",
        "key": "reportsToColumn",
        "dependsOn": "dataProvider",
        "options": {
          "source": "dataProvider",
          "from": "provider"
        },
        "info": "Must be a relationship column. Will use the first related row as a parent-node"
      },
      {
        "type": "text",
        "label": "Top of tree",
        "key": "topOfTree"
      },
      {
        "section": true,
        "name": "Layout",
        "settings": [
{
        "type": "number",
        "label": "Card width",
        "key": "cardWidth",
        "defaultValue": 180
      },
      {
        "type": "number",
        "label": "Card height",
        "key": "cardHeight",
        "defaultValue": 240
      },
      {
        "type": "number",
        "label": "Sibling spacing",
        "key": "siblingSpacing",
        "defaultValue": 40
      },
      {
        "type": "number",
        "label": "Children spacing",
        "key": "childrenSpacing",
        "defaultValue": 80
      }
        ]
      }
      
    ]
  }
}
